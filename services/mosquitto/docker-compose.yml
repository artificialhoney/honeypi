services:
  mosquitto:
    image: eclipse-mosquitto:latest
    container_name: mosquitto
    restart: unless-stopped
    ports:
      - 1883:1883
      - 9001:9001
    volumes:
      - ${HONEYPI_HOME}/mosquitto/config:/mosquitto/config
      - ${HONEYPI_HOME}/mosquitto/data:/mosquitto/data
      - ${HONEYPI_HOME}/mosquitto/log:/mosquitto/log
      - ./config/mosquitto.conf:/mosquitto/config/mosquitto.conf
    extends:
      file: ../../commons/compose.yml
      service: base