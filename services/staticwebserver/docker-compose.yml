services:
  staticwebserver:
    image: joseluisq/static-web-server:${HONEYPI_APP_VERSION:-latest}
    container_name: staticwebserver
    restart: unless-stopped
    extends:
      file: ../../commons/compose.yml
      service: base
    volumes:
      - ${HONEYPI_HOME}/staticwebserver/public:/public