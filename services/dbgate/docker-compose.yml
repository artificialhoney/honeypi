services:
  dbgate:
    image: dbgate/dbgate:${HONEYPI_APP_VERSION:-latest}
    restart: always
    container_name: dbgate
    volumes:
      - ${HONEYPI_HOME}/dbgate/data:/root/.dbgate
    extends:
      file: ../../commons/compose.yml
      service: base
    env_file:
      - ./service.env
